# Prompton Starter

Reference docs site for [Prompton](https://prompton.dev) — conversational Starlight on Cloudflare.

```bash
pnpm install
pnpm dev
```

Toggle **Browse / Chat** in the header. Deploy with `pnpm deploy` after configuring Vectorize + KV IDs in `wrangler.jsonc`.
